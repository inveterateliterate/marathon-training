// Unit test files can be added to this directory, or any directory nested within it.
// Filenames should have the format {name}.test.js.

test('Unit tests are being run correctly', () => {
  expect(true).toBe(true)
})